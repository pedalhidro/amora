.env
.DS_Store
node_modules/
*.xlsx
*__pycache__*
backend/pi/data/
venv/
*.pyc

# Runtime state served by the Pi backend — backup target, not source.
web/data/
web/photos/
web/clips/

# Checksums do último sync local<->bucket (scripts/sync-guard.sh) — por
# máquina, não versionado.
.sync-state/
web/tour_assets/